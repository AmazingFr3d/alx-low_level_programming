#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase, then in uppercase, followed by a new line
(*
 * Return: 0 on success
 */
int main(void)
{
	char lower = 'a';
	char upper = 'A';

	while (lower <= 'z')
	{
		putchar(lower);
		c++;
	}
	while (d <= 'Z')
	{
		putchar(upper);
		d++;
	}
	putchar('\n');
	return (0);

}
