#include "stdlib.h"
#include "main.h"
/**
 * _abs - Gets the absolute value of an integer
 * @x: the integer value to work with
 * Return: The absolute values
 */
int _abs(int x)
{
	int abs_val = abs(x);
	
	return (abs_val);
}
